# Build-script approvals for pnpm 10+/11 (the package.json "pnpm" field is ignored).
# esbuild needs its postinstall to fetch its native binary (Vite depends on esbuild);
# without approval a clean install (e.g. Vercel CI) fails with ERR_PNPM_IGNORED_BUILDS.
allowBuilds:
  esbuild: true
